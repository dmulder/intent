# Generated by Intent. Do not edit directly.
# Edit intent.yaml and regenerate this profile.
#include <tunables/global>

profile regression-service /usr/libexec/regression-service {
  # application.executable: profile attachment and executable read access.
  /usr/libexec/regression-service r,

  # config paths[0]: read access to /etc/regression-service.
  /etc/regression-service r,
  /etc/regression-service/** r,

  # cache paths[0]: read-write access to /var/cache/regression-service.
  /var/cache/regression-service rwk,
  /var/cache/regression-service/** rwk,

  # state paths[0]: read-write access to /var/lib/regression-service.
  /var/lib/regression-service rwk,
  /var/lib/regression-service/** rwk,

  # runtime paths[0]: read-write access to /run/regression-service.
  /run/regression-service rwk,
  /run/regression-service/** rwk,

  # outbound network[0]: outbound https connection to api.example.test:443.
  network inet stream,
  network inet6 stream,

  # outbound network[1]: outbound tcp connection to metrics.example.test:8443.
  network inet stream,
  network inet6 stream,

  # outbound network[2]: outbound udp connection to resolver.example.test:53.
  network inet dgram,
  network inet6 dgram,

  # Unix socket[0]: connect to client socket /run/dbus/system_bus_socket.
  unix (connect, send, receive) type=stream peer=(addr="/run/dbus/system_bus_socket"),
  /run/dbus/system_bus_socket rw,

  # Unix socket[1]: create and use server socket /run/regression-service/control.sock.
  unix (create, bind, listen, accept, send, receive) type=stream addr="/run/regression-service/control.sock",
  /run/regression-service/control.sock rw,

  # D-Bus ownership[0]: own system bus name org.example.Regression.
  dbus bind bus=system name="org.example.Regression",

  # D-Bus communication[0]: talk to system bus service org.freedesktop.DBus.
  dbus send bus=system peer=(name="org.freedesktop.DBus"),
  dbus receive bus=system peer=(name="org.freedesktop.DBus"),

  # capabilities[0]: Linux capability dac-override.
  capability dac_override,

  # capabilities[1]: Linux capability net-bind-service.
  capability net_bind_service,
}
