# Generated by Intent. Do not edit directly.
# Suggested SELinux file contexts. Review before installing.

# application.executable: executable entry point.
/usr/libexec/himmelblaud -- gen_context(system_u:object_r:himmelblaud_exec_t,s0)

# config paths[0]: label /etc/himmelblaud.
/etc/himmelblaud(/.*)? gen_context(system_u:object_r:himmelblaud_config_t,s0)

# cache paths[0]: label /var/cache/himmelblaud.
/var/cache/himmelblaud(/.*)? gen_context(system_u:object_r:himmelblaud_cache_t,s0)

# state paths[0]: label /var/lib/himmelblaud.
/var/lib/himmelblaud(/.*)? gen_context(system_u:object_r:himmelblaud_state_t,s0)

# runtime paths[0]: label /run/himmelblaud.
/run/himmelblaud(/.*)? gen_context(system_u:object_r:himmelblaud_runtime_t,s0)

# Unix socket[0]: label server socket /run/himmelblaud/socket.
/run/himmelblaud/socket -s gen_context(system_u:object_r:himmelblaud_runtime_t,s0)

# WARNING: Unix socket[1] is a client socket; Intent will not relabel peer-owned path /run/systemd/resolve/io.systemd.Resolve.
